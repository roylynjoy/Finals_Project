specVersion: "v1"
serviceId: "fieldmate"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fieldmate-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
